{
  "name": "eclipse-project",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "axios": "^1.6.0"
  },
  "scripts": {
    "build": "npm install && node build.js",
    "start": "npx serve -s build -l 3000"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
